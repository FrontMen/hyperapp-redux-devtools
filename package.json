{
  "name": "hyperapp-redux-devtools",
  "version": "0.0.1",
  "description": "higher order app to connect state to redux-devtools",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "prepare": "npm run build",
    "test": "jest --coverage --no-cache"
  },
  "keywords": ["hyperapp", "redux", "devtools"],
  "author": "Lasse Capel <lasse@frontmen.nl>",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^2.6.2"
  },
  "dependencies": {
    "hyperapp": "^1.0.1"
  }
}
